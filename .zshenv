export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
export DOCKER_HOST=unix://$HOME/docker.sock
